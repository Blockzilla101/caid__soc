{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run tests",
            "type": "shell",
            "command": "cd tests/cocotb && clear && bash run_tests.sh",
            "problemMatcher": [
                "$python"
            ]
        },
        {
            "label": "Build GCC programs",
            "type": "shell",
            "command": "cd tests/gcc && clear && bash build.sh",
        },
        {
            "label": "Build ASM programs",
            "type": "shell",
            "command": "cd tests/assembly && clear && bash build.sh",
        }
    ]
}